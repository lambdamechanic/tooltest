[package]
name = "tooltest-test-support"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
rmcp = { workspace = true, features = ["client"] }
serde_json = "1"
tokio = { version = "1", features = ["sync"] }
