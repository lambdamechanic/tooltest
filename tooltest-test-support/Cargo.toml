[package]
name = "tooltest-test-support"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[dependencies]
rmcp = { version = "0.12", features = ["client"] }
serde_json = "1"
tokio = { version = "1", features = ["sync"] }
